#ifndef LED_MODULE_H
#define LED_MODULE_H

/**
 * @brief Met à jour l'état de la LED Rouge (A) selon le mode global (manuel/auto).
 */
void updateLedStateA();

/**
 * @brief Met à jour l'état de la LED Verte (B) selon le mode global (manuel/auto).
 */
void updateLedStateB();

#endif
